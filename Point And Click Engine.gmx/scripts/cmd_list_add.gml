///cmd_list_add(command,id,data,...)

var args;

for(var i = 0;i<16;i++)
{
    if i < argument_count
    {
        args[i] = argument[i];
    }
    else
    {
        args[i] = NULL;
    }
}


var cmd,obj,data;
ds_list_add(cmdList,)
